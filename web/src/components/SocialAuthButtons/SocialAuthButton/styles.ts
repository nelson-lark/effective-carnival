import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    padding: "8px 21px",
  },
  label: {
    fontSize: 14,
    textTransform: "capitalize",
  },
}));
